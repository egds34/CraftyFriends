# Tasks

- [/] Store Overhaul
    - [ ] Create `types/store.ts` (Product, CartItem schemas)
    - [ ] Create `lib/mock-products.ts` (Data for Memberships, Boosts, Chat, etc.)
    - [ ] Create `components/providers/cart-provider.tsx` (Context, state, persist logic)
    - [ ] Update `app/layout.tsx` to include `CartProvider`
    - [ ] Create `components/store/store-navbar.tsx` (Sticky secondary nav with scrollspy)
    - [ ] Create `components/store/product-card.tsx` (UI for individual items)
    - [ ] Create `components/store/sticky-cart.tsx` (Floating sidebar cart for store page)
    - [ ] Refactor `app/store/page.tsx` (Implement grid layout with nav, products, and sticky cart)
    - [ ] Update `components/navbar-client.tsx` (Add cart icon/badge pointing to store or opening sidebar)
